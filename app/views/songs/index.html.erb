<h1>Songs Index</h1>

<% @songs.each do |song| %>

<!-- <li><%= link_to(song.title, song_path(song.id)) %></li> -->
<!-- line 5 does the same thing as line 7 -->
<li><%= link_to(song.title, song) %> <%= link_to "Edit", edit_song_path(song)%>
 <%= link_to "Delete", song, method: :delete %>
</li>

<% end %>
<br>
<h3>
  <%= link_to("New Song", new_song_path) %>
</h3>
